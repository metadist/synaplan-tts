# Synaplan TTS — Environment Configuration
#
# Copy this file to .env and adjust for your environment.
# .env is gitignored — safe for secrets and per-host config.
#
# ┌─────────────────────────────────────────────────────────┐
# │  Local dev:  No .env needed. Defaults to 127.0.0.1.    │
# │  GPU server: Copy to .env and set your LAN IP.         │
# └─────────────────────────────────────────────────────────┘

# Bind address — which IP the TTS service listens on.
# ⚠  NEVER use 0.0.0.0 — always bind to a specific interface.
#
#   127.0.0.1   = localhost only (default, safe for dev)
#   10.0.1.10   = GPU server internal LAN
#
TTS_BIND_ADDRESS=10.0.1.10
